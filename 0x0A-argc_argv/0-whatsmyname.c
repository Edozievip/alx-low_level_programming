#include "main.h"
#include <stdio.h>

/*
 * main - print the name
 * @argc: argiement count
 * @argv: arguement vector
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	(void) arg;
	printf("%s\n", argv[0]);
	return (0);
}
